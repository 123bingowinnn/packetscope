services:
  - type: web
    name: packetscope-live-lab
    env: python
    plan: free
    pythonVersion: "3.12"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 -m packetscope web --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    autoDeploy: true
